services:
  portfolio:
    build: ./app
    container_name: portfolio
    restart: unless-stopped
    ports:
      - "127.0.0.1:3010:3010"
    environment:
      - NODE_ENV=production
      - PORT=3010
