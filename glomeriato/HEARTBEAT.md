# HEARTBEAT.md

# Keep this file empty (or with only comments) to skip heartbeat API calls.

# Daily Tasks
- [ ] If time is between 08:00 and 09:00 AND you haven't sent the briefing today: Run `python3 morning_brief.py` and send the output to Jorg. Log completion in memory to avoid duplicates.
